import { Navigate, Route, Routes, useLocation } from "react-router-dom";
import { RouteObject, useRoutes } from "react-router-dom";

export default function PublicRoutes() {
  return useRoutes([
    // {
    //   path: "/",
    //   element: <Route to="home" />,
    // },
  ]);
}
